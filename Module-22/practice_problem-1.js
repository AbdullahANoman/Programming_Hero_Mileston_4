// একটা কোড লিখো। যেটা দিয়ে কোন একটা array এর মধ্যে সবচেয়ে ছোট সংখ্যা বের করে দিতে পারবে। 
var array = [10,20,30];

for(var i=0; i<array.length; i++)
{
    var element1= array[i]
    if(element1<array[i+1])
    {

    }
}